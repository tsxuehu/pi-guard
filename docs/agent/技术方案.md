```
┌─────────────────────────────────────────────────────┐
│                    PiGuard-Agent                    │
├─────────────────────────────────────────────────────┤
│  [配置管理]  [日志管理]  [性能监控]                     │
├─────────────────────────────────────────────────────┤
│  [采集层]          [处理层]          [输出层]          │
│  - VideoCapture    - MotionDetect   - RTMPPusher    │
│  - AudioCapture    - Encoder        - FileWriter    │
│  - AudioPlayback   - EchoCancel     - HTTPNotify    │
├─────────────────────────────────────────────────────┤
│  [通信层]                                            │
│  - WebSocketClient (控制+语音下行)                    │
├─────────────────────────────────────────────────────┤
│  [线程管理]  [队列管理]  [内存池]                       │
└─────────────────────────────────────────────────────┘
```


数据流 
```
                    ┌─────────────────────────────────────────────────────────────┐
                    │                        数据流全景                            │
                    └─────────────────────────────────────────────────────────────┘

USB摄像头 ──→ VideoCapture ──┬──→ MotionDetect ──┬──→ HTTPNotify ──→ Node.js
                             │                   │
                             │                   └──→ FileWriter ──→ 磁盘(MP4)
                             │
                             └──→ Encoder ──┬──→ RTMPPusher ──→ SRS
                                            │
麦克风 ──→ AudioCapture ──┬──→ EchoCanceller ┘
                          │        ↑
                          │        │ 参考信号
                          └────────┴──→ AudioPlayback ←── WebSocketClient ←── Node.js
                                                                     ↑
                                                                     │ 控制指令
                                                     WebSocketClient ←┘
```